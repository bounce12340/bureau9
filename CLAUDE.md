# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 這是什麼倉庫

這不是軟體專案，是「九局宇宙」（行政院第九局，Bureau 9）台灣諜報喜劇連載小說的**稿件庫**。內容全部是繁體中文散文（`.md` / `.txt`），加上一頁靜態索引 `index.html`（GitHub Pages，靠根目錄 `.nojekyll` 停用 Jekyll）。沒有 build、沒有測試框架、沒有 CI、沒有相依套件。

工作內容通常是：**寫新集數、潤稿、改設定集、更新排程表、維護 index.html**。所有正文最終目的地是 Threads 貼文串。

## 動手前必讀

改任何正文之前先讀 `07_設定集與準則/` 這三份，它們是唯一權威來源，優先於 README 的節錄：

| 檔案 | 管什麼 |
|---|---|
| `發文格式準則_v1.1.md` | 標題格式、檔案編號、元資料標頭、hashtag、禁用事項、真實案件改編原則 |
| `2026_05_05_九局宇宙_綜合寫作準則_情緒停頓強化版_v3_1.md` | 世界觀法則、敘事技術、情緒停頓寫法、各角色聲腔、場景感官、§15 校驗清單 |
| `2026_07_14_..._角色設定集與故事線全覽_v7.md` | 角色設定（外觀／背景／關係）與各故事線梗概，寫任何角色前先對照 |

`發文排程總表.md` 是《春壤篇》×《刀與針》交錯發文的唯一順序依據；發完一集在 ✓ 欄打勾，其他地方不要另記進度。**該表任何文字不得貼上 Threads**。

## 目錄與依存關係

```
01_本傳全文/      本傳純小說全文（.txt，非 Threads 格式）
02_人物前傳/      各角色前傳：_Threads發文全集 = 正文；_故事線全覽 = 大綱
03_依晴線/        依晴篇 + 依晴本傳《觀察者的反擊》
04_番外與行動/    盟寵特攻隊、國會說服指南
05_網師四部曲/    反派本傳正文 ×4 + 全文合集 + 完整分集細綱 + 時間軸對齊
06_春壤篇/        四部曲外傳正文 + 分集架構 + 故事線設計
07_設定集與準則/  上表四份
```

幾個容易踩到的耦合：

- `05_網師四部曲/網師四部曲_全文合集.md` 是四部正文的**合併副本**。改任一部的 `_Threads發文.md`，合集要一起改，否則兩邊分岔。
- `網師四部曲_完整分集細綱.md` 與 `_時間軸對齊.md` 鎖定了部序（回收 → 少康 → 結網 → 刀與針，非編年）與世界內時序，並與大石前傳、依晴本傳的時點咬合。動劇情時點要回頭核對這兩份。
- 設定集 v7 的關係圖譜、格式準則的角色縮寫表（`Net`／`Suo`／`Yi`…）會隨新角色增修；新增角色時兩邊都要補，並升版號。
- `index.html` 的作品卡片是**手寫死的**（`data-cat`／`data-kw` 加上指向 GitHub blob 的 URL-encoded 連結），新增作品檔案時要手動補一張卡，並更新 header 的作品數。

## 稿件格式（正文硬性規則）

每集 = 8 則貼文（6–7 則是下限不是上限），每則 **≤500 中文字**（不計空白），實務控在 420–460 字。**情緒不能為了字數被壓扁——超了就多開一則貼文，不是刪停頓。**

新稿沿用 `05_`／`06_` 的排版慣例：

```
═══════════════════════════════
## 第一集〈接手〉（共 8 則貼文）
═══════════════════════════════

──── 貼文 1/8 ────

【網師・春壤（一）— 接手】

檔案編號：B9-Enemy-Soil-01
主角：謝明遠 / 國家安全部・台灣站（對外：政大交換研究生）
地點：台北・政治大學
狀態：接手一畦種了很多年的地 ／ 外殼是讀書會，內核是生產線

（正文……）
```

`02_`／`03_` 的舊檔用 `---` 分隔貼文、無 `──── 貼文 n/8 ────` 標記；沿用該檔既有慣例，不要為了統一而重排舊稿。

- 檔案編號：`B9-{類型}-{角色縮寫}-{序號}`，類型為 `Origin`/`Mission`/`Classified`/`Side`/`War`/`Profile`/`Enemy`。
- 集內只有 `## 第 N 集` 用 Markdown 標題；**貼文正文裡不得出現 `#`／`##`**（Threads 不渲染）。
- Hashtag 只放在整篇結尾標記之後，正文中一律不出現。固定四個 `#行政院第九局 #九局宇宙 #台灣小說 #連載小說`，再依類型／角色加。
- 結尾標記：待續 `【{系列標籤}（{集序}）．待續】`／完結 `【{系列標籤}．完】`。

## 寫作鐵律（違反了就是改壞）

- **零破折號**（`——`）：停頓一律用句點＋換行處理。標題格式裡的 `—` 分隔符除外。
- **禁巧合副詞**推進關鍵劇情：突然／恰好／忽然／剛好／正好／碰巧。
- **Show, don't tell**：不得直接寫「他很難過」「大家很緊張」；停頓要有物件、聲音或微動作，停頓後要接一個動作或轉折。
- 荒謬手段 × 嚴肅目的必須**並置**，不是輪流。核銷、統編、發票、廟會、藍白拖是世界秩序的一部分。
- 動物（威士忌、公主）**無對白**，也不以愛國心／使命感行動：威士忌走食物邏輯，公主走資源最大化邏輯。
- 角色聲腔照準則 §12 對照表（高伯水墨留白不解釋、梅姐帳一定要算清楚、網師織造／棋局比喻且比高伯更冷更慢…）。
- 四部曲鐵律：**網師與白手（高伯）永不見面**，收在〈隔海一桌〉的隔海對弈，不是相見也不是和解。
- 取材真實案件（如《少康》取自少康專案）時，走格式準則 §十：史實可作錨點、會被賦予大量虛構對白的真實人物改用虛構姓名、不把虛構言行安到已歿真人口中、涉及行刑等真實悲劇的章節不以喜劇處理。

一集寫完／潤完，逐項跑準則 §15 的快速校驗清單。

## 稿件檢查指令

沒有 lint，用這些代替（`貼文` 標記格式的檔案適用）：

```bash
# 逐則字數（不計空白）與超標則號
python3 - <<'PY'
import re
p = '06_春壤篇/春壤篇_Threads發文.md'
posts = re.split(r'──── 貼文 \d+/\d+ ────', open(p, encoding='utf-8').read())[1:]
n = [len(re.sub(r'\s', '', x)) for x in posts]
print('則數', len(n), '最長', max(n))
print('超過500:', [(i+1, c) for i, c in enumerate(n) if c > 500])
PY

# 破折號（應為 0）
grep -n '——' 06_春壤篇/春壤篇_Threads發文.md

# 巧合副詞（逐筆人工判斷是否推動關鍵劇情）
grep -n '突然\|恰好\|忽然\|剛好\|正好\|碰巧' 06_春壤篇/春壤篇_Threads發文.md

# 集數與則數盤點
grep -c '──── 貼文' 05_網師四部曲/刀與針_Threads發文.md
```

`index.html` 直接用瀏覽器開檔即可驗（純靜態、無建置步驟），或 `python3 -m http.server` 起一個本機伺服器。

## 語言

倉庫內所有文件、正文、commit message 一律**繁體中文**（README.en.md 是唯一的英文對照，改 README.md 時同步更新它）。對白的台語氣口、里長／廟會／遶境等在地機構、用語要經在地校準。
